import React, { Component } from 'react'
import { withStyles } from '@material-ui/core'

import styles from './style'

class TarkBoard extends Component{
    render() {
        return (
            <div>
            </div>
        )
    }
}
export default withStyles(styles)(TarkBoard);