import React, { Component } from 'react'
import { withStyles } from '@material-ui/styles'
import styles from './style'

class SideBar extends Component{
    render() {
        return <div>SideBar Board</div>
    }
}

export default withStyles(styles)(SideBar);
